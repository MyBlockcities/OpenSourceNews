# Static frontend (Vite + Caddy). API service: see nixpacks.api.toml and docs/DEPLOYMENT.md.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "caddy file-server --listen :$PORT --root /app/dist"
